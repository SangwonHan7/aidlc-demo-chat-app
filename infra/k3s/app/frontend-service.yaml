# Frontend Service - NodePort 30080 -> containerPort 3000
apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: quickchat-app
spec:
  type: NodePort
  selector:
    app: frontend
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30080
